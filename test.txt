-- -----------------------------------------------------------------------------
-- Script name:   AVSA_VAR_Logged_60D.hql
-- Created on:    12/11/2015
-- Authors:       Calabrese, A. - P.Melchiorre
-- Purpose:       Process to calculate the VAR Logged 60D KPIs
-- -----------------------------------------------------------------------------

-- creating a temporary table to store the 1^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp01 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  accesses_60D INT,
  number_used_platforms_60D INT,
  number_used_devices_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp01';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp01
-- accesses_60D
-- number_used_platforms_60D
-- number_used_devices_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp01
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.accesses_60D,
    tmp.number_used_platforms_60D,
    tmp.number_used_devices_60D
from
    var_logged_master vlm, (select
    user_id,
    count(accesses_60D) AS accesses_60D,
    count(distinct(number_used_platforms_60D)) AS number_used_platforms_60D,
    count(distinct(number_used_devices_60D)) AS number_used_devices_60D

from (

select

    final.user_id1 AS user_id,
    final.user_id2 AS accesses_60D,
    final.channel AS number_used_platforms_60D,
    final.deviceid AS number_used_devices_60D

from (
select t1.user_id AS user_id1, t2.userid AS user_id2, t2.channel, t2.deviceid
from
    var_logged_master t1
    left outer join
    (select * from target_login t3 where  t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.userid) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 2^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp02 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  playlists_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp02';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp02
-- playlists_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp02
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.playlists_60D
from
    var_logged_master vlm, (select
    user_id,
    count(playlists_60D) AS playlists_60D

from (

select

    final.user_id1 AS user_id,
    (if(final.eventtype = "FAVOURITECREATION", final.user_id2, null)) AS playlists_60D

from (
select t1.user_id AS user_id1, t2.user_id AS user_id2, t2.eventtype
from
    var_logged_master t1
    left outer join
    (select * from target_favourite t3 where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.user_id) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 3^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp03 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  impressions_60D INT,
  viewed_adv_60D INT,
  clicks_through_60D INT,
  skipped_adv_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp03';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp03
-- impressions_60D
-- viewed_adv_60D
-- clicks_through_60D
-- skipped_adv_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp03
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.impressions_60D,
    tmp.viewed_adv_60D,
    tmp.clicks_through_60D,
    tmp.skipped_adv_60D
from
    var_logged_master vlm, (select
    user_id,
    count(impressions_60D) AS impressions_60D,
    count(viewed_adv_60D) AS viewed_adv_60D,
    count(clicks_through_60D) AS clicks_through_60D,
    count(skipped_adv_60D) AS skipped_adv_60D

from (

select

    final.user_id1 AS user_id,
    final.user_id2 AS impressions_60D,
    (if(final.completion_percentage == 100, final.user_id2, null)) AS viewed_adv_60D,
    (if(final.has_been_clicked == "Y", final.user_id2, null)) AS clicks_through_60D,
    (if(final.has_been_skipped == "Y", final.user_id2, null)) AS skipped_adv_60D

from (
select t1.user_id AS user_id1, t1.birth_date, t1.gender, t2.user_id AS user_id2, t2.completion_percentage, t2.has_been_clicked, t2.has_been_skipped
from
    var_logged_master t1
    left outer join
    (select * from target_impression t3 where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.user_id) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 4^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp04 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  likes_60D INT,
  dislikes_60D INT,
  ratings_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp04';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp04
-- likes_60D
-- dislikes_60D
-- ratings_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp04
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.likes_60D,
    tmp.dislikes_60D,
    tmp.ratings_60D
from
    var_logged_master vlm, (select
    user_id,
    sum(likes_60D) AS likes_60D,
    sum(dislikes_60D) AS dislikes_60D,
    sum(ratings_60D) AS ratings_60D

from (

select

    final.user_id1 AS user_id,
    (if(final.is_liked = "Y", 1, 0)) AS likes_60D,
    (if(final.is_disliked = "Y", 1, 0)) AS dislikes_60D,
    (if(final.rating !="", 1, 0)) AS ratings_60D

from (
select t1.user_id AS user_id1, t1.birth_date, t1.gender, t2.user_id AS user_id2, t2.is_liked, t2.is_disliked, t2.rating
from
    var_logged_master t1
    left outer join
    (select * from target_curation t3  where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.user_id) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 5^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp05 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  npvr_requests_60D INT,
  npvr_views_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp05';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp05
-- npvr_requests_60D
-- npvr_views_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp05
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.npvr_requests_60D,
    tmp.npvr_views_60D
from
    var_logged_master vlm, (select
    user_id,
    count(npvr_requests_60D) AS npvr_requests_60D,
    count(npvr_views_60D) AS npvr_views_60D

from (

select

    final.user_id1 AS user_id,
    (if(final.recording_action = "CREATE", final.user_id2, null)) AS npvr_requests_60D,
    (if(final.recording_action = "PLAY", final.user_id2, null)) AS npvr_views_60D

from (
select t1.user_id AS user_id1, t2.user_id AS user_id2, t2.recording_action
from
    var_logged_master t1
    left outer join
    (select * from target_npvr t3 where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.user_id) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 6^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp06 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  purchases_60D INT,
  revenues_60D DOUBLE
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp06';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp06
-- purchases_60D
-- revenues_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp06
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    vlm.first_view_date,
    tmp.purchases_60D,
    tmp.revenues_60D
from
    var_logged_master vlm, (select
    user_id,
    count(purchases_60D) AS purchases_60D,
    sum(revenues_60D) AS revenues_60D

from (

select

    final.user_id1 AS user_id,
    final.user_id2 AS purchases_60D,
    final.originalprice AS revenues_60D

from (
select t1.user_id AS user_id1, t2.userid AS user_id2, t2.originalprice
from
    var_logged_master t1
    left outer join
    (select * from target_purchase t3 where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.userid) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 7^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp07 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  reminders_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp07';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp07
-- reminders_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp07
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.reminders_60D
from
    var_logged_master vlm, (select
    user_id,
    count(reminders_60D) AS reminders_60D

from (

select

    final.user_id1 AS user_id,
    final.user_id2 AS reminders_60D

from (
select t1.user_id AS user_id1, t2.userid AS user_id2
from
    var_logged_master t1
    left outer join
    (select * from target_reminder t3 where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.userid) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 8^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp08 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  searches_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp08';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp08
-- searches_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp08
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.searches_60D
from
    var_logged_master vlm, (select
    user_id,
    count(searches_60D) AS searches_60D

from (

select

    final.user_id1 AS user_id,
    final.user_id2 AS searches_60D

from (
select t1.user_id AS user_id1, t2.user_id AS user_id2
from
    var_logged_master t1
    left outer join
    (select * from target_search t3 where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.user_id) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 9^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp09 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  video_drop_off_60D INT,
  average_video_drop_off_60D DOUBLE,
  launched_apps_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp09';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp09
-- video_drop_off_60D
-- average_video_drop_off_60D
-- launched_apps_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp09
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.video_drop_off_60D,
    tmp.average_video_drop_off_60D,
    tmp.launched_apps_60D
from
    var_logged_master vlm, (select
    user_id,
    count(video_drop_off_60D) AS video_drop_off_60D,
    (count(average_video_drop_off_60D_num1)/count(average_video_drop_off_60D_num2)) AS average_video_drop_off_60D,
    count(launched_apps_60D) AS launched_apps_60D

from (

select

    final.user_id1 AS user_id,
    (if(final.eventtype == "VIDEOEND" and final.terminationreason == "ERROR", final.sessionid, null)) AS video_drop_off_60D,
    (if(final.eventtype == "VIDEOEND" and final.terminationreason == "ERROR", final.sessionid, null)) AS average_video_drop_off_60D_num1,
    final.user_id2 AS average_video_drop_off_60D_num2,
    (if(final.eventtype == "APPLICATIONSTARTUP", final.sessionid, null)) AS launched_apps_60D

from (
select t1.user_id AS user_id1, t2.userid AS user_id2, t2.sessionid, t2.eventtype, t2.terminationreason
from
    var_logged_master t1
    left outer join
    (select * from target_user_action t3 where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.userid) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 10^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp10 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  video_views_60D INT,
  video_playback_duration_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp10';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp10
-- video_views_60D
-- video_playback_duration_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp10
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.video_views_60D,
    tmp.video_playback_duration_60D
from
    var_logged_master vlm, (select
    user_id,
    count(video_views_60D) AS video_views_60D,
    round(sum(video_playback_duration_60D)/60) AS video_playback_duration_60D

from (

select

    final.user_id1 AS user_id,
    (if(final.download != "Y", final.user_id2, null)) AS video_views_60D,
    (if(final.download != "Y", final.consumption, null)) AS video_playback_duration_60D

from (
select t1.user_id AS user_id1, t2.userid AS user_id2, t2.download, t2.consumption
from
    var_logged_master t1
    left outer join
    (select userid, consumption, download from target_watching t3, target_category_map t4
where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')
and t3.contentid = t4.contentid) t2
    on t1.user_id = t2.userid
) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 11^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp11 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  top_watched_category_60D STRING
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp11';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp11
-- TOP_WATCHED_CATEGORY_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp11
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.categoryname AS top_watched_category_60D
from
    var_logged_master vlm
    left outer join
    (select S.userid, S.categoryname from (
select userid,categoryname,num,rank() over (partition by userid order by num desc,categoryname) AS r
from (
select t1.userid, t3.categoryname, count(t1.userid) AS num
          from target_profiling t1, target_watching t2, target_category_map t3
       where t2.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t2.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')
           and t1.userid = t2.userid and t2.contentid = t3.contentid
group by t1.userid, t3.categoryname) a) S
  where S.r=1) tmp
    on vlm.user_id = tmp.userid;

-- creating a temporary table to store the 12^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp12 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  live_views_60D INT,
  live_playback_duration_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp12';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp12
-- live_views_60D
-- live_playback_duration_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp12
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.live_views_60D,
    tmp.live_playback_duration_60D
from
    var_logged_master vlm, (select
    user_id,
    count(live_views_60D) AS live_views_60D,
    round(sum(live_playback_duration_60D)/60) AS live_playback_duration_60D

from (

select

    final.user_id1 AS user_id,
    (if(final.download != "Y", final.user_id2, null)) AS live_views_60D,
    (if(final.download != "Y", final.consumption, null)) AS live_playback_duration_60D

from (
select t1.user_id AS user_id1, t2.userid AS user_id2, t2.download, t2.consumption
from
    var_logged_master t1
    left outer join
    (select userid, consumption, download from target_watching t3, target_epg_catalog t4
where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')
and t3.contentid = t4.livecontentid) t2
    on t1.user_id = t2.userid
) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- creating a temporary table to store the 13^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp13 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  top_watched_channel_60D STRING
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp13';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp13
-- top_watched_channel_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp13
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.channel_name AS top_watched_channel_60D
from
    var_logged_master vlm
    left outer join
    (select S.userid, S.channel_name from (
select userid,channel_name,num,rank() over (partition by userid order by num desc,channel_name) AS r
from (
select t1.userid, t3.channel_name, count(t1.userid) AS num
          from target_profiling t1, target_watching t2, target_epg_catalog t3
       where t2.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t2.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')
           and t1.userid = t2.userid and t2.contentid = t3.livecontentid
group by t1.userid, t3.channel_name) a) S
  where S.r=1) tmp
    on vlm.user_id = tmp.userid;

-- creating a temporary table to store the 14^ subset of VAR Logged's KPIs
CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D_tmp14 (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  views_60D INT,
  playback_duration_60D INT,
  downloads_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D_tmp14';

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D_tmp14
-- views_60D
-- playback_duration_60D
-- downloads_60D
-- -----------------------------------------------------------------------------

INSERT OVERWRITE TABLE var_logged_60D_tmp14
select
    vlm.date,
    vlm.user_id,
    vlm.birth_date,
    vlm.gender,
    vlm.age_bracket,
    vlm.country,
    vlm.state,
    vlm.province,
    vlm.city,
    vlm.zip_code,
    vlm.registration_method,
    vlm.referral_source,
    vlm.registration_trigger,
    vlm.registration_date,
    vlm.most_used_platform,
    vlm.first_access_date,
    vlm.first_view_date,
    tmp.views_60D,
    tmp.playback_duration_60D,
    tmp.downloads_60D
from
    var_logged_master vlm, (select
    user_id,
    count(views_60D) AS views_60D,
    round(sum(playback_duration_60D)/60) AS playback_duration_60D,
    count(downloads_60D) AS downloads_60D

from (

select

    final.user_id1 AS user_id,
    (if(final.download != "Y", final.user_id2, null)) AS views_60D,
    (if(final.download != "Y", final.consumption, null)) AS playback_duration_60D,
    (if(final.download = "Y", final.user_id2, null)) AS downloads_60D

from (
select t1.user_id AS user_id1, t2.userid AS user_id2, t2.download, t2.consumption
from
    var_logged_master t1
    left outer join
    (select * from target_watching t3 where t3.dt_reference >= DATE_SUB(FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd') , 60) 
      AND t3.dt_reference < FROM_UNIXTIME(UNIX_TIMESTAMP(),'yyyy-MM-dd')) t2
    on t1.user_id = t2.userid) final) a
group by user_id) tmp
where vlm.user_id = tmp.user_id;

-- -----------------------------------------------------------------------------
-- Temporary table name: var_logged_60D
-- join all temporary tables
-- -----------------------------------------------------------------------------

CREATE EXTERNAL TABLE IF NOT EXISTS var_logged_60D (
  date STRING,
  user_id STRING,
  birth_date DATE,
  gender STRING,
  age_bracket STRING,
  country STRING,
  state STRING,
  province STRING,
  city STRING,
  zip_code STRING,
  registration_method STRING,
  referral_source STRING,
  registration_trigger STRING,
  registration_date STRING,
  most_used_platform STRING,
  first_access_date STRING,
  first_view_date STRING,
  accesses_60D INT,
  number_used_platforms_60D INT,
  number_used_devices_60D INT,
  video_views_60D INT,
  video_playback_duration_60D INT,
  live_views_60D INT,
  live_playback_duration_60D INT,
  views_60D INT,
  playback_duration_60D INT,
  top_watched_category_60D STRING,
  top_watched_channel_60D STRING,
  downloads_60D INT,
  playlists_60D INT,
  impressions_60D INT,
  viewed_adv_60D INT,
  clicks_through_60D INT,
  skipped_adv_60D INT,
  likes_60D INT,
  dislikes_60D INT,
  ratings_60D INT,
  searches_60D INT,
  reminders_60D INT,
  video_drop_off_60D INT,
  average_video_drop_off_60D DOUBLE,
  launched_apps_60D INT,
  purchases_60D INT,
  revenues_60D DOUBLE,
  npvr_requests_60D INT,
  npvr_views_60D INT
)
ROW FORMAT
DELIMITED FIELDS TERMINATED BY '\073'
LINES TERMINATED BY '\n'
STORED AS TEXTFILE
LOCATION '${hivevar:root_path}/agg/VAR_LOGGED/var_logged_60D';

INSERT OVERWRITE TABLE var_logged_60D
SELECT t1.*,
       t10.video_views_60D,
       t10.video_playback_duration_60D,
       t12.live_views_60D,
       t12.live_playback_duration_60D,
       t14.views_60D,
       t14.playback_duration_60D,
       t11.top_watched_category_60D,
       t13.top_watched_channel_60D,
       t14.downloads_60D,
       t2.playlists_60D,
       t3.impressions_60D,
       t3.viewed_adv_60D,
       t3.clicks_through_60D,
       t3.skipped_adv_60D,
       t4.likes_60D,
       t4.dislikes_60D,
       t4.ratings_60D,
       t8.searches_60D,
       t7.reminders_60D,
       t9.video_drop_off_60D,
       t9.average_video_drop_off_60D,
       t9.launched_apps_60D,
       t6.purchases_60D,
       t6.revenues_60D,
       t5.npvr_requests_60D,
       t5.npvr_views_60D
FROM var_logged_60D_tmp01 t1
JOIN var_logged_60D_tmp02 t2 ON t1.user_id = t2.user_id
JOIN var_logged_60D_tmp03 t3 ON t1.user_id = t3.user_id
JOIN var_logged_60D_tmp04 t4 ON t1.user_id = t4.user_id
JOIN var_logged_60D_tmp05 t5 ON t1.user_id = t5.user_id
JOIN var_logged_60D_tmp06 t6 ON t1.user_id = t6.user_id
JOIN var_logged_60D_tmp07 t7 ON t1.user_id = t7.user_id
JOIN var_logged_60D_tmp08 t8 ON t1.user_id = t8.user_id
JOIN var_logged_60D_tmp09 t9 ON t1.user_id = t9.user_id
JOIN var_logged_60D_tmp10 t10 ON t1.user_id = t10.user_id
JOIN var_logged_60D_tmp11 t11 ON t1.user_id = t11.user_id
JOIN var_logged_60D_tmp12 t12 ON t1.user_id = t12.user_id
JOIN var_logged_60D_tmp13 t13 ON t1.user_id = t13.user_id
JOIN var_logged_60D_tmp14 t14 ON t1.user_id = t14.user_id;
